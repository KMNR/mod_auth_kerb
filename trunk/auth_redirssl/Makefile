##
##  Makefile -- Build procedure for sample auth_redirssl Apache module
##  Autogenerated via ``apxs -n auth_redirssl -g''.
##

APXS=apxs

all: mod_auth_redirssl.so

mod_auth_redirssl.so: mod_auth_redirssl.c 
	$(APXS) -c $(CPPFLAGS) $(CFLAGS) $(LDFLAGS) mod_auth_redirssl.c

clean:
	-rm -f mod_auth_redirssl.o mod_auth_redirssl.lo mod_auth_redirssl.slo mod_auth_redirssl.la 
